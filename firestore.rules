rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TESTING RULES: Allows all reads and writes
    // Use this to confirm if the issue is auth-timing or project-connection
    match /{document=**} {
      allow read, write: if true;
    }
  }
}