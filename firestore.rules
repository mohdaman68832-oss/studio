
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TESTING MODE: Allows all read/write operations for development.
    // WARNING: Do not use this in a production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
