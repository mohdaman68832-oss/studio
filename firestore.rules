rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // GLOBAL TESTING MODE: Full access for all operations to prevent permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}