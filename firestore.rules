
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isAuth() {
      return request.auth != null;
    }

    // User Profiles: Public read, Owner write
    match /userProfiles/{userId} {
      allow read: if true;
      allow write: if isAuth() && request.auth.uid == userId;
    }

    // Private Chats
    match /privateChats/{chatId} {
      allow read: if isAuth() && (request.auth.uid in resource.data.participants);
      allow create: if isAuth() && (request.auth.uid in request.resource.data.participants);
      allow update: if isAuth() && (request.auth.uid in resource.data.participants);

      match /messages/{messageId} {
        allow read, write: if isAuth();
      }
    }
    
    // Posts
    match /posts/{postId} {
      allow read: if true;
      allow write: if isAuth();

      match /likes/{userId} {
        allow read: if true;
        allow write: if isAuth() && request.auth.uid == userId;
      }

      match /suggestions/{suggestionId} {
        allow read: if true;
        allow write: if isAuth();
      }

      match /views/{userId} {
        allow read: if true;
        allow write: if isAuth() && request.auth.uid == userId;
      }
    }

    // Reports: Any auth user can create, only admin (simulated here as any auth for MVP view) can read/manage
    match /reports/{reportId} {
      allow create: if isAuth();
      allow read, update, delete: if isAuth();
    }

    // Follows
    match /follows/{followId} {
      allow read: if true;
      allow write: if isAuth() && (request.auth.uid == request.resource.data.followerId || (resource != null && request.auth.uid == resource.data.followerId));
    }

    // Groups
    match /groups/{groupId} {
      allow read: if true;
      allow write: if isAuth();
    }
    
    // Global Fallback (Secure by default)
    match /{path=**} {
      allow read, write: if isAuth();
    }
  }
}
