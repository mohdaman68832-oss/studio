rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Testing Mode: Open access to all collections to eliminate permission errors during development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
