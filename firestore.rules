rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TESTING MODE: Sab kuch open hai taaki koi permission ya list error na aaye.
    // WARNING: Production mein ise badalna zaroori hai.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
