
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "email": { "type": "string" },
        "profilePictureUrl": { "type": "string" },
        "bannerUrl": { "type": "string" },
        "bio": { "type": "string" },
        "stickers": { "type": "array" },
        "customColors": { "type": "object" },
        "createdAt": { "type": "string" },
        "isOnline": { "type": "boolean" },
        "isBanned": { "type": "boolean" },
        "lastActive": { "type": "string" },
        "interests": { "type": "array" }
      },
      "required": ["id", "username", "email"]
    },
    "PrivateChat": {
      "title": "PrivateChat",
      "type": "object",
      "properties": {
        "chatId": { "type": "string" },
        "participants": { "type": "array" },
        "lastMessage": { "type": "string" },
        "timestamp": { "type": "string" },
        "unreadCounts": { "type": "object", "description": "Map of userId to unread message count" }
      },
      "required": ["chatId", "participants"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "type": "object",
      "properties": {
        "senderId": { "type": "string" },
        "text": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["senderId", "text", "createdAt"]
    },
    "Report": {
      "title": "Report",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "reporterId": { "type": "string" },
        "targetId": { "type": "string" },
        "targetType": { "type": "string", "enum": ["post", "profile", "message"] },
        "reason": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "resolved"] },
        "createdAt": { "type": "string" }
      },
      "required": ["reporterId", "targetId", "targetType", "reason"]
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "/userProfiles/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "Stores user profiles with customization."
    },
    "/privateChats/{chatId}": {
      "schema": { "$ref": "#/backend/entities/PrivateChat" },
      "description": "Stores metadata for private one-to-one chats."
    },
    "/privateChats/{chatId}/messages/{messageId}": {
      "schema": { "$ref": "#/backend/entities/ChatMessage" },
      "description": "Stores individual messages within a private chat."
    },
    "/reports/{reportId}": {
      "schema": { "$ref": "#/backend/entities/Report" },
      "description": "Stores content reports from users."
    }
  }
}
